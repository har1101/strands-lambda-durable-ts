# 現状報告と引き継ぎ事項

最終更新: 2026-09-25（前回: [2026-09-24-handoff.md](./2026-09-24-handoff.md)）

## 1. 全体像

| リポジトリ | 役割 | 状態 |
| --- | --- | --- |
| [har1101/strands-lambda-durable-functions](https://github.com/har1101/strands-lambda-durable-functions) | ライブラリ本体（OSS）。Strands Agents の**非公式**コミュニティ拡張。パッケージ名も `strands-lambda-durable-functions` | 公開済み。最新は `v0.2.0` の GitHub Release（tarball を添付）。npm には公開していない |
| [har1101/minamo](https://github.com/har1101/minamo) | 依存ゼロの軽量 OSS。AI エージェントのループを durable にするコア（7 章） | `@minamojs/minamo` と `@minamojs/lambda-df` の `0.1.0-alpha.0` を npm 公開済み。CI（Node 22/24）は green |
| [har1101/strands-lambda-durable-ts](https://github.com/har1101/strands-lambda-durable-ts)（このリポジトリ） | デプロイできるチャットアプリのサンプル（Strands 版と minamo 版のワーカー）、調査メモ、minamo の解説記事 | `main` にマージ済み（PR #1〜#5） |

ライブラリは、このリポジトリの `packages/strands-lambda-durable` から `git filter-branch --subdirectory-filter` で切り出しました。コミット履歴も引き継いでいます。2026-09-24 に、何と連携するかが名前で分かるように `strands-lambda-durable` から `strands-lambda-durable-functions` へ改名しました（v0.2.0）。旧リポジトリの URL は GitHub がリダイレクトします。v0.1.x の旧名の tarball も Release に残っています。このリポジトリのバックエンドは、v0.2.0 の release tarball を読み込みます（`examples/chat/backend/package.json`）。

## 2. ライブラリ（har1101/strands-lambda-durable-functions）

- README の冒頭に、非公式のコミュニティ拡張であること（Strands Agents プロジェクトや AWS とは提携しておらず、承認もサポートも受けていないこと）と、フォークではない追加モジュールであることを明記しています。`package.json` の description と GitHub の説明も「Unofficial ... extension」にしています。
- 構成: `src/`（`codec`、`retry`、`events`、`scope`、`model`、`tool`、`executor`、`workflow-tool`、`interrupts`、`mcp`、`offload`、`s3`、`index`）、`test/`（19 件。すべて `LocalDurableTestRunner` で実行し、AWS は不要）。
- README は英語（`README.md`）と日本語（`README.ja.md`）の 2 つで、相互にリンクしています。ほかに `CHANGELOG.md`、`CONTRIBUTING.md`、`LICENSE`（MIT）があります。
- CI: `.github/workflows/ci.yml`。Node 22/24 で typecheck、test、build、`npm pack --dry-run` を実行します。
- リリース: `.github/workflows/release.yml`。`v*` タグを push すると、テストの後に `npm pack` を実行し、GitHub Release を作って tarball を添付します。**`NPM_TOKEN` シークレットがある場合だけ** `npm publish --provenance` も実行します。
- チェックポイント形式は `schemaVersion` 3 です。v3 では、ツール実行ごとの `appStateDelta` を記録します。v1/v2 のレコード（`appState` の全体スナップショット）も読めます。

### npm 公開の方針: 当面は公開しない（最終的には npm で公開する）

最終的には npm に公開し、利用者が semver の範囲指定（`^0.2.0` など）でバージョンを管理できるようにします。それまでに、次の準備を進めます。

- 公開 API を安定させる。上流の `InterruptError` と `ToolExecutor` が公開されたら、公開された型を使う形に切り替える（9 章）。
- 依存を最小限にしたコア（7 章）との関係を決め、パッケージの構成と名前を確定させる。公開後は名前を変えにくいため。
- Trusted Publishing で公開できるようにする（下記）。
- CHANGELOG と semver の運用を続ける。破壊的変更はマイナーバージョンを上げ、チェックポイント形式を変えるときは `schemaVersion` を上げる。

トラブルの起きにくさを優先して、`NPM_TOKEN` は設定せず、GitHub Release の tarball で配布します。理由は次のとおりです。

- 長期間有効なトークンをリポジトリに置かないので、漏えいのリスクがありません。
- npm では、公開したバージョンを後から取り消せません（unpublish は 72 時間以内などの制限付き）。名前も一度使うと実質的に固定されます。

利用者は `npm install https://github.com/har1101/strands-lambda-durable-functions/releases/download/v0.2.0/strands-lambda-durable-functions-0.2.0.tgz` でインストールします。

将来公開する場合の手順:

1. npm の名前が空いているか確認します（2026-09-24 時点では空き）。
2. トークンを使わない npm の Trusted Publishing（GitHub Actions の OIDC）を優先して検討します。npmjs.com のパッケージ設定でこのリポジトリのワークフローを登録し、`release.yml` の publish ステップを OIDC 用に変えます。最初の公開の前に登録できるかどうかは、公開時に npm のドキュメントで確認してください（未検証）。トークンを使う場合は、`gh secret set NPM_TOKEN -R har1101/strands-lambda-durable-functions` を実行してから次のタグを push します。
3. 公開後、このリポジトリの backend の依存を `"strands-lambda-durable-functions": "^0.2.x"` に変え、README 2 つの tarball URL の注記を削除します。

## 3. サンプルアプリ（このリポジトリ）

ワーカーは 2 種類から選べます。API、DynamoDB の形式、ライブイベント、フロントエンドは共通です。

- `examples/chat/backend/src/worker.ts`: Strands 版（`strands-lambda-durable-functions` v0.2.0）
- `examples/chat/backend/src/worker-minamo.ts`: minamo 版（`@minamojs/minamo` と `@minamojs/lambda-df` の `0.1.0-alpha.0`、Bedrock ConverseStream を直接呼ぶ手書きのループ）
- 共通部分: `worker-common.ts`（`WorkerRequest`、`validate`、システムプロンプト、ツールの説明文、注文データ、返金 ID の計算）
- 選び方: 環境変数 `WORKER_ENGINE=strands|minamo`（既定は strands）。`scripts/build.mjs` が選んだワーカーを `dist/worker` にバンドルし、`deploy.sh` が `config.json` に `engine` を書き込みます。フロントエンドはタイトルを `Durable Strands Chat` / `Durable minamo Chat` に切り替えます。

**2026-09-24 夕方の作業は [PR #5](https://github.com/har1101/strands-lambda-durable-ts/pull/5) でマージ済み（merge commit `02655e2`、CI green）**: minamo 版ワーカー、共通化、deploy.sh の SAM CLI 不要化、smoke の engine 対応、フロントの engine 表示、CI の minamo ビルド、README、ブログ記事 `docs/blog/minamo-guide.md`。コミットは 3 つ（refactor、feat、docs）。ブログの手直しはユーザーが後で行い、公開は minamo がもう少し成熟してからです。

### デプロイ済みのスタック（us-east-1、アカウント 064212132988）

| 項目 | strands-durable-chat | minamo-durable-chat |
| --- | --- | --- |
| SiteUrl | https://dfpxqdkbp5wdf.cloudfront.net/ | https://d3jbtv0vpt0puz.cloudfront.net/ |
| UserPoolId / Client | `us-east-1_myAQ2SBlX` / `3b3stv2kvbah4iq8lvda3prl8l` | `us-east-1_aSUIlCq3Z` / `6s84796vovno66blr980999ucp` |
| Cognito ドメイン | `strands-durable-064212132988` | `minamo-durable-064212132988` |
| Worker エイリアス | `arn:aws:lambda:us-east-1:064212132988:function:strands-durable-chat-worker:live` | `arn:aws:lambda:us-east-1:064212132988:function:minamo-durable-chat-worker:live` |
| テーブル | `strands-durable-chat-ConversationsTable-1857P6ZITJL38`、`strands-durable-chat-MessagesTable-GBLP7OQ7NVT8` | `minamo-durable-chat-ConversationsTable-1UT9Y71RWT003`、`minamo-durable-chat-MessagesTable-138PL8QTJC14X` |
| CloudFront / サイト用バケット | `E1NMB9CN2Q3TJ8` / `strands-durable-chat-sitebucket-3yd1ov57jctw` | `E1RSBN3JXRQ36Z` / `minamo-durable-chat-sitebucket-wtkka8ymsnmx` |
| Cognito ユーザー | haruki-fukuchi@nec.com（一時パスワードをメールで送付済み） | haruki-fukuchi@nec.com（2026-09-24 に招待メールを送付。E2E 用のテストユーザーは削除済み） |

- コードのアップロード先: `durable-chat-artifacts-064212132988-us-east-1`（deploy.sh が作成）。
- 両スタックとも 2026-09-24 に作業ツリーのコードで再デプロイ済み。
- 動作確認（2026-09-24）:
  - smoke 3 種（replay、approval、parallel）を両ワーカーで実行し、すべて `failures: []`。minamo 版は replay が invocations 2、approval が invocations 2（`approval` コンテキストの下に CALLBACK と submitter step、その後 `complete` step）、parallel が `tools-1:<toolUseId>` の 2 スコープ。Strands 版はリファクタリング後も従来どおり（`tool-issue_refund-*` が 2 回、`tools-1-0`/`tools-1-1`）。
  - minamo 版をヘッドレス Chromium で E2E: 並列照会のライブ表示、承認待ちのリロード後の保持、承認で返金 ID 発行、却下で `rejected`、同じ会話の 2 ターン目で履歴を使った回答、ヘッダーの `Durable minamo Chat` 表示。Strands 版のブラウザ E2E は 2026-09-23 が最後。
  - コールドスタート計測のため、両ワーカーに不正なペイロードで `coldstart-*` という名前の実行を 10 件ずつ開始しました。すべて `FAILED`（`Invalid userId`）で、保持期間（7 日）後に消えます。
- 再デプロイ手順:
  1. `aws login` で認証します（`default` プロファイル）。セッションは約 15 分で切れます。
  2. `AWS_REGION=us-east-1 examples/chat/scripts/deploy.sh`（Strands 版）、`WORKER_ENGINE=minamo AWS_REGION=us-east-1 examples/chat/scripts/deploy.sh`（minamo 版）。SAM CLI は不要です（`aws cloudformation package`/`deploy`）。シェルの既定値は `AWS_REGION=ap-northeast-1` なので、必ず us-east-1 を明示してください。2 つを同時に実行しないでください（`dist/` を共有します）。
  3. smoke: `WORKER_ALIAS_ARN`、`CONVERSATIONS_TABLE`、`MESSAGES_TABLE` を設定し、minamo 版は `WORKER_ENGINE=minamo` も付けて `npm run smoke`、`smoke:approval`、`smoke:parallel`（`-w @strands-lambda-durable/example-chat-backend`）。
- 削除: サイト用とオフロード用のバケットを空にしてから、`aws cloudformation delete-stack --stack-name <スタック名>`。最後に `aws s3 rb s3://durable-chat-artifacts-064212132988-us-east-1 --force`。

### ブログ記事

`docs/blog/minamo-guide.md`（日本語、約 1,100 行）。AWS に詳しいがソフトウェア開発の経験が少ない読者向けに、minamo の思想と設計原則、TypeScript が Lambda で動くまで、durable functions のチェックポイントとリプレイ、SDK（2.4.0）の内部と `@minamojs/lambda-df` の対応、チャット例の使い方、パフォーマンスチューニング（実測値つき）をまとめています。ユーザーの書式指定: `**` の強調なし、英数字の前後に半角スペースなし、主語を明確にした能動態、図は mermaid、出典リンク必須。

## 4. PR とレビュー

- PR #1（ライブラリとサンプル）: CodeRabbit の指摘 7 件（初回 6 件、再レビュー 1 件）にすべて対応しました。スレッドは CodeRabbit 側で解決済みです。CI（Node 22/24 とサンプル）はすべて green で、merge commit でマージしました。
- PR #2（スタンドアロンライブラリへの切り替えと、引き継ぎ・学びのドキュメント）: 依頼どおり CodeRabbit を通さずにマージしました。切り替えは、ローカルで `npm run typecheck` と `npm run build`、依存の重複排除（`npm ls`）を確認しています。
- PR #3（`docs/context/` への移動、omp の context-sync 拡張、`AGENTS.md`/`CLAUDE.md`）: CodeRabbit を通さずにマージしました。

## 5. Strands 本体へのコントリビューション

| 対象 | 状態 | 次の作業 |
| --- | --- | --- |
| `InterruptError` の export | Issue [#4540](https://github.com/strands-agents/harness-sdk/issues/4540) と PR [#4541](https://github.com/strands-agents/harness-sdk/pull/4541) を作成済み。2026-09-24 にメンテナー @liramon2 から「looks good overall」とレビューがあり、指摘 2 件に commit `79492d24e` で対応して push しました。(1) `interrupt.ts` の `InterruptError` の TSDoc を元の 2 行に戻す（ファイル内の他のコメントより詳しすぎるため）。(2) エージェントを動かすテストを削除し、既存の `exports error classes` テストに `expect(SDK.InterruptError).toBeDefined()` を 1 行追加。両スレッドに返信し（スレッドは未解決のまま）、PR 本文の Changes も更新済み。2026-09-25 時点で `REVIEW_REQUIRED` | メンテナーの再レビューと approve 待ちです。追加の指摘があれば、フォーク `har1101/harness-sdk` のブランチ `feat/ts-export-interrupt-error` で対応します |
| `ToolExecutor` 基底クラスの公開 | [#762 にコメント](https://github.com/strands-agents/harness-sdk/issues/762#issuecomment-5797836885)して、方向性の合意を求めています。実装はブランチ `har1101:feat/ts-public-tool-executor`（commit `6a32117`）に push 済みです | CONTRIBUTING の「significant work はメンテナーの確認を待つ」に従い、**PR は未作成**です。合意が得られたら PR を作成します。本文の要点は下の付録にあります。`api/needs-review` ラベルは外部コントリビューターでは付けられないため、PR 本文でメンテナーに付与を依頼します |

`ToolExecutor` ブランチの確認済み事項:

- husky の pre-commit（build、coverage 付き test 4763 件、lint、format）が通っています。
- `npm run type-check` が通っています。
- docs のスニペットは `site/test-snippets` の typecheck を通ります。`pino` のエラーは既存のもので、この変更とは無関係です。
- 公開 API だけを使うスクリプトで、独自スケジューラーの動作を確認しました。割り込み後に再開しても、完了済みのツールは再実行されません。

作業用の clone は `/home/vscode/workspace/harness-sdk`（remote `origin` はフォーク `har1101/harness-sdk`）です。`feat/ts-export-interrupt-error` を checkout 済みで、コミット作者はリポジトリローカルの `git config` で `har1101 <174846912+har1101@users.noreply.github.com>` に設定しています。git の HTTPS 認証は `gh auth setup-git` で gh に任せています。`ToolExecutor` の作業用だった `/tmp/strands-contrib` は消えているので、必要ならフォークのブランチ `feat/ts-public-tool-executor` を checkout してください。

## 6. 未決事項

- **ライセンス**: MIT のままとします（個人プロダクトのため、組織の承認は不要）。
- **npm 公開**: 最終的には公開します。それまでは tarball で配布します（2 章に準備事項）。公開するときは Trusted Publishing を優先します。
- **Strands の community catalog と、AWS Durable Execution の integrations ページへの掲載申請**: npm 公開後に行うのが自然です。

## 7. minamo（依存を最小限にした軽量 OSS）

2026-09-24 に名前を **minamo（水面）** に決め、[har1101/minamo](https://github.com/har1101/minamo) を public で作りました。設計は [docs/design.ja.md](https://github.com/har1101/minamo/blob/main/docs/design.ja.md) にまとめてあり、これが正です。README は英語と日本語の 2 つです。

- **ユーザーが決めたスコープ**:
  - 依存は最小限にします。コアは npm の依存も `node:` の API も使いません。
  - 対象のエンジンは、最優先が Lambda durable functions、次が Cloudflare Workflows までです。
  - AI SDK や Strands のアダプターは作りません。エンジンのアダプターが SDK を任意の peer dependency として参照するのは「互換」なので問題ありません。
  - `strands-lambda-durable-functions` は別のライブラリとして残し、置き換えません。
- **構成**:
  - `packages/core/src/index.ts`: `@minamojs/minamo`。`Durable`（`step`、`scope`、`signal`）、宣言的な `Retry`、JSON + `Uint8Array` のコーデック、`model()`、`runTools()`、`RetryableError`。npm 依存なし。
  - `packages/core/src/memory.ts`: `@minamojs/minamo/memory`。クラッシュを注入できるテスト用エンジン。
  - `packages/lambda-df/src/index.ts`: `@minamojs/lambda-df`。peer dependencies は `@minamojs/minamo` と `@aws/durable-execution-sdk-js`。
  - npm workspaces のモノレポ。2 パッケージは同じバージョンで公開します。ユーザーの最終選択は `@minamojs/core` ではなく **`@minamojs/minamo`** です。
- **確認済み**:
  - commit `3755b69`: 改名後の build、typecheck、test（5 件）、pack、Lambda サンプルの build/import が成功。tarball を別ディレクトリへインストールし、公開 import とクラッシュ後のリプレイも確認。CI run `35974121308` は success。
  - **デプロイした Lambda での実行**（`examples/lambda-bedrock`。承認と却下の両方で `SUCCEEDED`）。詳細は下の「デプロイ済みの例」を見てください。
- **デプロイ済みの例**（us-east-1、アカウント 064212132988）:

  | 項目 | 値 |
  | --- | --- |
  | スタック | `minamo-example` |
  | 関数 / エイリアス | `minamo-example-agent` / `arn:aws:lambda:us-east-1:064212132988:function:minamo-example-agent:live` |
  | コンソール | https://us-east-1.console.aws.amazon.com/lambda/home?region=us-east-1#/functions/minamo-example-agent |
  | コード用のバケット | `minamo-artifacts-064212132988-us-east-1`（`deploy.sh` が作成） |
  | モデル | `us.anthropic.claude-haiku-4-5-20251001-v1:0` |

  - 手順: リポジトリのルートで `npm install` を実行し、`examples/lambda-bedrock` で `AWS_REGION=us-east-1 npm run deploy` を実行します。SAM CLI は不要です。最後に `AWS_REGION=us-east-1 npm run smoke`（または `-- reject`）を実行します。モノレポ分割後の承認スモークも `SUCCEEDED`（invocations 3、モデル記録 2）。最終パッケージ改名後は再デプロイせず、bundle の読み込みまで確認しました。
  - 結果（承認した場合）:
    - モデル呼び出しの記録は `model-1` と `model-2` の 2 つで、どちらも 1 回だけ実行されました。
    - 呼び出し（invocation）は 3 回でした。
    - 2 つのツールのスコープは、呼び出し順に開きました。
    - `get_weather` は 1 回目に `RetryableError` で失敗し、2 回目に成功しました。
    - `issue_refund` は `approval` のコールバックを待ってから、`refund` の step を実行しました。
  - 削除: `aws cloudformation delete-stack --stack-name minamo-example` を実行し、続けて `aws s3 rb s3://minamo-artifacts-064212132988-us-east-1 --force` を実行します。
- **既定のリトライ**:
  - `step` は、`retry` を指定しなければ 1 回だけ実行します。Lambda の SDK の既定のリトライは無効にしています。
  - `model()` は、一時的なエラーだけを最大 4 回まで試行します。
  - ツールは、`RetryableError` だけを最大 3 回まで試行します。
- **npm 公開**:
  - `minamo` は既存の `minami`、`minio` に似ているため npm に 403 で拒否されました。ユーザーが org `minamojs` を作成し、最終的に `@minamojs/minamo` と `@minamojs/lambda-df` に決定しました。
  - 2026-09-24 に両方の `0.1.0-alpha.0` を `--access public --tag alpha` で公開しました。registry の alpha タグ反映と、別ディレクトリでの `npm install @minamojs/minamo@alpha @minamojs/lambda-df@alpha @aws/durable-execution-sdk-js` が成功。インストールしたパッケージの import・クラッシュ後リプレイも確認済みです。
  - 最終命名の変更は `3755b69` として `main` に push 済みです。
  - `.github/workflows/release.yml` は `v*` タグで両パッケージを pack し、GitHub Release に添付、未公開バージョンを npm OIDC で公開します。
  - GitHub Release: https://github.com/har1101/minamo/releases/tag/v0.1.0-alpha.0 。タグは `3755b69` を指し、2 つの tarball を添付済みです。
  - Release run `35974413569` は再実行で成功しました。初回は npm metadata の反映前だったため公開済み判定が外れましたが、反映後は既公開の両パッケージをスキップしました。OIDC による新バージョン公開そのものは未検証です。
  - **Trusted Publishing 設定済み（2026-09-24、ユーザー報告）**: 両パッケージで GitHub owner `har1101`、repository `minamo`、workflow `release.yml` を登録しました。直接の `npm publish` を許可し、Environment name は空欄です。Publishing access は「Require two-factor authentication and disallow bypass 2fa tokens」。設定後の OIDC 新バージョン公開はまだ実行していません。
- **次の作業**:
  1. 次の実際のリリースで、両パッケージのバージョンを更新し、対応する `v*` タグを push して OIDC 公開を確認します。設定確認だけの新バージョン公開は行いません。
  2. 必要になったら S3 オフロードを追加します。次のエンジン候補は `@minamojs/cloudflare` です。
  3. チャット例とブログ執筆で見つかった点を Issue にしました（SDK 2.4.0 の `dist/index.mjs` で確認。未修正）:
     - [#1](https://github.com/har1101/minamo/issues/1)（bug）: `signal` の submitter の step に SDK 既定のリトライ（合計 6 回、5〜60 秒）が効く。`retryStrategy: () => ({ shouldRetry: false })` を渡すのが候補。
     - [#2](https://github.com/har1101/minamo/issues/2)（enhancement）: `model()` が step の記録の 256KB 上限を検査しない。テキスト差分の圧縮かサイズ検査が候補。実測は 24〜31 イベントで 1.9〜2.4KB。
     - [#3](https://github.com/har1101/minamo/issues/3)（enhancement）: `ToolContext` にツール呼び出し ID がない。チャット例は `idempotencyKey` の最後の `#` 以降を取り出している。
     - [#4](https://github.com/har1101/minamo/issues/4)（documentation）: `lambda-df` と design.ja.md の「DurablePromise は遅延評価」という説明が不正確。SDK は呼んだ時点で開始する。
     - [#5](https://github.com/har1101/minamo/issues/5)（bug）: `backoffRate: undefined` を明示すると SDK の既定値を上書きし、2 回目以降のリトライ待ちが NaN 秒になる（SDK 単体で再現済み）。
- **未決事項**:
  - Lambda のオペレーション名の長さの上限: Bedrock の `toolUseId` を含む名前は動きました。上限の値そのものは未確認です。
  - Standard Schema に対応するかどうか
- 作業用の clone は `/home/vscode/workspace/minamo` にあります。以前のスパイク（`durable-agent-spike`）は、このディレクトリに移しました。

## 8. 作業記録の自動化

- `.omp/extensions/context-sync.ts`（oh-my-pi の拡張）が、メインセッションの終了直前（`session_stop`）に動きます。`docs/context/` の最後のコミットより新しい作業があれば、エージェントに次の作業を依頼します。
  1. `docs/context/<日付>-handoff.md` と `<日付>-learnings.md` を更新する。
  2. `docs/context/` だけをコミットして push する。

  規約は [README.md](./README.md) にあります。
- 動作確認:
  - 一時リポジトリで判定ロジックを 10 パターン検証しました。新規コミット、同じ状態の再判定、docs のみのコミット、未コミットの変更、無効化などです。
  - 一時リポジトリで実際に `omp -p` を実行し、拡張が自動で読み込まれることを確認しました。エージェントが handoff と learnings を作成し、`docs/context/` だけをコミットしました（push はリモートがないため失敗）。
- omp 以外のハーネスでは自動化されません。`AGENTS.md`（Claude Code 向けには `CLAUDE.md` から読み込み）に、同じ手順を手動で行うよう書いてあります。

## 9. 既知の制約

- 並列ツールが同じ `appState` のキーに書き込んだ場合、最終的な値は完了順に依存します（README に記載済み）。
- `InterruptError` は現在、`error.name === "InterruptError"` で判定しています。上流で export されたら `instanceof` に切り替えます（`src/tool.ts` の `isInterruptError`）。
- `DurableToolExecutor` は `ConcurrentToolExecutor` を継承しており、型は `Parameters<ConcurrentToolExecutor["execute"]>` で取り出しています。上流で `ToolExecutor` が公開されたら、公開型に切り替えます。
- ライブの `appState` を追跡するため、エージェントごとの `StateStore` インスタンスの `set`、`delete`、`clear` を差し替えています。Strands が StateStore の実装を変えた場合は、`test/replay.test.ts` と `test/features.test.ts` の appState 系テストで検出できます。

## 付録: ToolExecutor PR 本文の要点

- タイトル: `feat(tools): make the TypeScript ToolExecutor base public`
- 公開するもの: `ToolExecutor`（値）、`ToolExecutorOptions`、`ToolExecutionInput`（型）。
- 型の拡張: `AgentConfig.toolExecutor` と `Agent.toolExecutor` の型を `ToolExecutor | ToolExecutorStrategy` に広げます。
- 名前の変更: protected の `_storePendingToolExecution` を `storePendingToolExecution` に変えます。`strands-ts/AGENTS.md` では、`_` 接頭辞は private にだけ使う規約だからです。
- `ToolExecutorOptions` のうちエージェントだけが設定するフィールド（バックグラウンドタスクと interrupt の差し替え）は、`@internal` とし、1 行の説明を付けます。
- `execute()` の契約:
  - ツール実行ごとに結果を 1 つ push します。
  - 再開時は `completedToolResults` を再利用します。
  - `cancelSignal` を尊重します。
  - `InterruptError` のときは `storePendingToolExecution()` を呼んでから再 throw します。
- レビュアーに判断を求める点:
  - `middlewareRegistry`、`tracer`、`meter` の型が export されていない。
  - 名前の変更。
  - `executeBackground()` は internal のまま残す。
- テスト:
  - `src/tools/executors/__tests__/executor.test.ts`: 逆順に実行するスケジューラー。
  - `src/__tests__/index.test.ts`: `ToolExecutor` が export され、組み込みの 2 つのエグゼキューターの基底クラスであること。
- ドキュメント: `site/.../tools/executors.mdx` に TypeScript のカスタムエグゼキューターの節を追加します（`TimedToolExecutor` の例）。
